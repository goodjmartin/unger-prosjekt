# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 8
      uses: actions/setup-java@v2
      with:
        java-version: '8'
        distribution: 'adopt'
        cache: maven
    - name: Build with Maven
      #run: mvn -B package --file pom.xml
      #run: mvn clean install -e -DskipTests
      run: cd unger-vaadin-core;echo "hello";echo "test1";pwd;echo "test2";dir ../;echo "hvordan gikk det?";echo "test3";ls ~/.m2/;echo "test3"; echo "heisann" >> hoho;ls;echo "gikk det?";less hoho;pwd ~/.m2/; 
